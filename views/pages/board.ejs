<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/styles/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@400;900&family=Roboto+Mono&family=Sarabun&display=swap" rel="stylesheet">
</head>
<body class="font-kanit bg-red-200 flex flex-col justify-center">
    <%- include('../partials/header') %>
    <div class="flex flex-col w-[80vw] text-center">
    <% let index = 0 %>
    <% locals.posts.forEach(element => { %>
        <a href="./<%= index %>" class="text-teal-900 my-4 py-2 bg-orange-300">
            <h1 class="text-2xl"><%= element.title %></h1>
        </a>
        <% index++ %>
    <% }); %>
    </div>
</body>
</html>